services:
  app:
    build: .
    ports:
      - "8000:8000"
    # Secrets (OpenAI key, session secret) live in a gitignored .env — never in
    # this file. The demo runs without it (AI feedback falls back to authored
    # content), so .env is optional.
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: "sqlite:////app/data/app.db"
    volumes:
      - app-data:/app/data

volumes:
  app-data:
